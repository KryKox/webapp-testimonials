import {Label} from "@/components/ui/label";
import {LoggedInButton} from "@/features/auth/LoggedInButton";

export const Header = () => {
    return (
        <div>
            <LoggedInButton />
        </div>
    );
};

